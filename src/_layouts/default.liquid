<!DOCTYPE html>
<html lang="en">
  <head>
    {% render "head", metadata: site.metadata, title: page.title %}
  </head>
  <body
    class="{{ page.layout }} {{ page.page_class }}"
    data-controller="theme"
    data-theme-target="switch"
  >
    {% render "navbar", metadata: site.metadata, page: page, resource: resource
    %}

    <main>
      {{ content }}
    </main>

    {% render "footer", metadata: site.metadata, page: page, resource: resource
    %}
  </body>
</html>
